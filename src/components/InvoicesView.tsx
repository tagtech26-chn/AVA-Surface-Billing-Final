import React, { useEffect, useMemo, useState } from 'react';
import { AlertCircle, Calendar, CheckCircle2, Clock, Eye, Printer, Receipt, RefreshCw, ShieldCheck, Tag, Timer, UserCheck } from 'lucide-react';
import { Invoice, InvoiceStatus, PaymentMethod } from '../types';
import { formatCurrency, formatDateTime } from '../lib/utils';
import { hydrateInvoicesFromServer } from '../lib/invoiceHydration';
import { Storage } from '../lib/storage';

interface InvoicesViewProps {
  invoices: Invoice[];
  onRecordPayment: (invoiceId: string, paymentAmount: number, method: PaymentMethod, notes?: string) => void;
  onProcessRefund: (invoiceId: string, restockItems: boolean) => void;
  onSelectInvoiceToPrint: (invoice: Invoice) => void;
  currencySymbol: string;
}
type CreditNoteViewStatus = 'NOT_REQUESTED' | 'PENDING' | 'APPROVED' | 'REJECTED';
type DocumentType = 'QUOTATIONS' | 'INVOICES';

export const InvoicesView: React.FC<InvoicesViewProps> = ({ invoices, onSelectInvoiceToPrint, currencySymbol }) => {
  const [documentType, setDocumentType] = useState<DocumentType>('QUOTATIONS');
  const [mode, setMode] = useState<'ALL' | 'TODAY'>('ALL');
  const [search, setSearch] = useState('');
  const [status, setStatus] = useState<'ALL' | InvoiceStatus>('ALL');
  const [selectedId, setSelectedId] = useState<string | null>(null);
  const [liveInvoices, setLiveInvoices] = useState<Invoice[]>(invoices);
  const [refreshing, setRefreshing] = useState(false);
  const activeUser = Storage.getUsers().find((user) => user.id === Storage.getActiveUserId());
  const canSeeCreditNote = activeUser?.role === 'ADMIN' || activeUser?.role === 'ACCOUNTANT' || activeUser?.role === 'MANAGER' || activeUser?.role === 'BRANCH_MANAGER';

  useEffect(() => { setLiveInvoices(invoices); }, [invoices]);
  const refreshFromServer = async () => { setRefreshing(true); try { const latest = await hydrateInvoicesFromServer(); if (latest.length > 0) setLiveInvoices(latest); } finally { setRefreshing(false); } };
  useEffect(() => { void refreshFromServer(); const timer = window.setInterval(() => void refreshFromServer(), 5000); return () => window.clearInterval(timer); }, []);

  const isFinalInvoice = (invoice: Invoice) => invoice.status === 'PAID' || invoice.status === 'REFUNDED' || invoice.workflowStatus === 'PAYMENT_CONFIRMED' || invoice.workflowStatus === 'COMPLETED';
  const isToday = (value: string) => { const date = new Date(value); const now = new Date(); return date.getFullYear() === now.getFullYear() && date.getMonth() === now.getMonth() && date.getDate() === now.getDate(); };
  const quotationCount = liveInvoices.filter((invoice) => !isFinalInvoice(invoice)).length;
  const invoiceCount = liveInvoices.filter(isFinalInvoice).length;
  const visibleInvoices = useMemo(() => { const query = search.trim().toLowerCase(); return liveInvoices.filter((invoice) => { if (documentType === 'INVOICES' ? !isFinalInvoice(invoice) : isFinalInvoice(invoice)) return false; if (mode === 'TODAY' && !isToday(invoice.date)) return false; if (status !== 'ALL' && invoice.status !== status) return false; if (!query) return true; return invoice.invoiceNumber.toLowerCase().includes(query) || (invoice.quotationNumber || '').toLowerCase().includes(query) || (invoice.customer?.name || '').toLowerCase().includes(query) || (invoice.customer?.phone || '').toLowerCase().includes(query) || (invoice.salespersonName || '').toLowerCase().includes(query) || invoice.items.some((item) => item.product.name.toLowerCase().includes(query) || item.product.sku.toLowerCase().includes(query)); }); }, [liveInvoices, documentType, mode, search, status]);
  useEffect(() => { if (visibleInvoices.length === 0) { setSelectedId(null); return; } if (!selectedId || !visibleInvoices.some((invoice) => invoice.id === selectedId)) setSelectedId(visibleInvoices[0].id); }, [visibleInvoices, selectedId]);
  const selected = visibleInvoices.find((invoice) => invoice.id === selectedId) || visibleInvoices[0] || null;

  const statusBadge = (value: InvoiceStatus) => value === 'PAID' ? <span className="inline-flex items-center gap-1 px-2 py-1 rounded-lg bg-emerald-500/10 text-emerald-300 border border-emerald-500/30 text-[10px] font-black"><CheckCircle2 className="w-3 h-3" />PAID</span> : value === 'PARTIAL' ? <span className="inline-flex items-center gap-1 px-2 py-1 rounded-lg bg-amber-500/10 text-amber-300 border border-amber-500/30 text-[10px] font-black"><Clock className="w-3 h-3" />PARTIAL</span> : value === 'REFUNDED' ? <span className="inline-flex items-center gap-1 px-2 py-1 rounded-lg bg-slate-500/10 text-slate-300 border border-slate-500/30 text-[10px] font-black">REFUNDED</span> : <span className="inline-flex items-center gap-1 px-2 py-1 rounded-lg bg-rose-500/10 text-rose-300 border border-rose-500/30 text-[10px] font-black"><AlertCircle className="w-3 h-3" />UNPAID</span>;
  const managerStatus = (invoice: Invoice) => { const workflow = invoice.workflowStatus || ''; if (['PAYMENT_PENDING','PAYMENT_CONFIRMED','COMPLETED'].includes(workflow)) return 'APPROVED'; if (workflow === 'MANAGER_APPROVAL_REJECTED') return 'REJECTED'; if (workflow === 'MANAGER_APPROVAL_PENDING') return 'PENDING'; return Number(invoice.branchManagerDiscountPercent || 0) > 0 || Number(invoice.branchManagerDiscountAmount || 0) > 0 || !!invoice.branchManagerRemarks ? 'APPROVED' : 'NOT REQUIRED'; };
  const managerBadge = (invoice: Invoice) => { const value = managerStatus(invoice); if (value === 'APPROVED') return <span className="inline-flex items-center gap-1 px-2 py-1 rounded-lg bg-emerald-500/10 text-emerald-300 border border-emerald-500/30 text-[10px] font-black"><UserCheck className="w-3 h-3" />MANAGER APPROVED</span>; if (value === 'PENDING') return <span className="inline-flex items-center gap-1 px-2 py-1 rounded-lg bg-amber-500/10 text-amber-300 border border-amber-500/30 text-[10px] font-black"><Timer className="w-3 h-3" />MANAGER PENDING</span>; if (value === 'REJECTED') return <span className="inline-flex items-center gap-1 px-2 py-1 rounded-lg bg-rose-500/10 text-rose-300 border border-rose-500/30 text-[10px] font-black"><AlertCircle className="w-3 h-3" />MANAGER REJECTED</span>; return <span className="text-[10px] font-black text-slate-500">NOT REQUIRED</span>; };
  const creditNoteStatus = (invoice: Invoice): CreditNoteViewStatus => { const amount = Number(invoice.creditNoteAmount || 0); if (amount <= 0) return 'NOT_REQUESTED'; if (invoice.workflowStatus === 'MANAGER_APPROVAL_PENDING') return 'PENDING'; if (['PAYMENT_PENDING','PAYMENT_CONFIRMED','COMPLETED'].includes(invoice.workflowStatus || '')) return 'APPROVED'; return 'REJECTED'; };
  const creditBadge = (value: CreditNoteViewStatus) => value === 'APPROVED' ? <span className="inline-flex px-2 py-1 rounded-lg bg-emerald-500/10 text-emerald-300 border border-emerald-500/30 text-[10px] font-black">APPROVED</span> : value === 'PENDING' ? <span className="inline-flex px-2 py-1 rounded-lg bg-amber-500/10 text-amber-300 border border-amber-500/30 text-[10px] font-black">PENDING</span> : value === 'REJECTED' ? <span className="inline-flex px-2 py-1 rounded-lg bg-rose-500/10 text-rose-300 border border-rose-500/30 text-[10px] font-black">REJECTED</span> : <span className="text-[10px] font-black text-slate-500">NOT REQUESTED</span>;

  const openDocumentView = (invoice: Invoice) => {
    const popupWindow = window.open('', '_blank', 'width=1200,height=900,scrollbars=yes,resizable=yes');
    if (!popupWindow) { window.alert('Please allow pop-ups to view the document.'); return; }
    const paid = isFinalInvoice(invoice); const label = paid ? 'INVOICE' : 'QUOTATION'; const number = invoice.invoiceNumber || invoice.quotationNumber || '—';
    const itemRows = (invoice.items || []).map((item) => `<tr><td>${item.product.name}</td><td>${item.quantity} ${item.selectedUnit || item.product.unit || ''}</td><td>${formatCurrency(Number(item.finalUnitPrice || 0), currencySymbol)}</td><td>${formatCurrency(Number(item.totalPrice || 0), currencySymbol)}</td></tr>`).join('');
    const itemDiscount = Number(invoice.itemDiscountsTotal || 0); const promoDiscount = Number(invoice.promoDiscountAmount || 0); const managerDiscount = Number(invoice.branchManagerDiscountAmount || 0); const totalDiscount = itemDiscount + promoDiscount + managerDiscount; const managerState = managerStatus(invoice); const creditState = creditNoteStatus(invoice); const paymentRef = invoice.paymentSpecificReference || invoice.paymentsHistory?.at(-1)?.referenceNumber || '—'; const paymentDate = invoice.paymentConfirmedAtUtc || invoice.paymentsHistory?.at(-1)?.date;
    const section = (title:string, body:string, cls='') => `<section class="section ${cls}"><h3>${title}</h3>${body}</section>`;
    const workflow = `<div class="workflow"><div class="step done"><b>1. POS Created</b><span>Quotation ${invoice.quotationNumber || '—'}</span></div><div class="step ${managerState === 'APPROVED' ? 'done' : managerState === 'PENDING' ? 'pending' : 'blocked'}"><b>2. Manager Approval</b><span>${managerState}</span></div><div class="step ${invoice.workflowStatus === 'PAYMENT_PENDING' || paid ? 'done' : 'pending'}"><b>3. Accounts</b><span>${paid ? 'PAYMENT CONFIRMED' : invoice.workflowStatus === 'PAYMENT_PENDING' ? 'READY FOR PAYMENT' : 'WAITING'}</span></div><div class="step ${paid ? 'done' : 'blocked'}"><b>4. Invoice</b><span>${paid ? invoice.invoiceNumber || 'GENERATED' : 'NOT GENERATED'}</span></div></div>`;
    const managerBody = `<div class="kv"><span>Status</span><strong>${managerState}</strong></div><div class="kv"><span>Additional discount</span><strong>${Number(invoice.branchManagerDiscountPercent || 0).toFixed(2)}% &nbsp; ${formatCurrency(managerDiscount,currencySymbol)}</strong></div><div class="kv"><span>Manager</span><strong>${invoice.branchManagerUserId || '—'}</strong></div><div class="remarks"><b>Remarks</b><p>${invoice.branchManagerRemarks || 'No manager remarks recorded.'}</p></div>`;
    const promoBody = `<div class="kv"><span>Promotion code</span><strong>${invoice.promoCodeApplied || 'None'}</strong></div><div class="kv"><span>Promotion discount</span><strong>${formatCurrency(promoDiscount,currencySymbol)}</strong></div>`;
    const creditBody = `<div class="kv"><span>Status</span><strong>${creditState}</strong></div><div class="kv"><span>Amount</span><strong>${formatCurrency(Number(invoice.creditNoteAmount || 0),currencySymbol)}</strong></div><div class="remarks"><b>Reason</b><p>${invoice.creditNoteReason || 'No credit note reason recorded.'}</p></div>`;
    const paymentBody = `<div class="kv"><span>Method</span><strong>${invoice.paymentMethodConfirmed || invoice.paymentMethod || '—'}</strong></div><div class="kv"><span>Reference / UTR</span><strong>${paymentRef}</strong></div>${paymentDate ? `<div class="kv"><span>Confirmed</span><strong>${formatDateTime(paymentDate)}</strong></div>` : ''}<div class="kv"><span>Amount</span><strong>${formatCurrency(Number(invoice.amountPaid || invoice.grandTotal || 0),currencySymbol)}</strong></div>`;
    const totalsBody = `<div class="kv"><span>Subtotal</span><strong>${formatCurrency(Number(invoice.subtotal || 0),currencySymbol)}</strong></div><div class="kv"><span>Item discount</span><strong>${formatCurrency(itemDiscount,currencySymbol)}</strong></div><div class="kv"><span>Promotion discount</span><strong>${formatCurrency(promoDiscount,currencySymbol)}</strong></div><div class="kv"><span>Manager discount</span><strong>${formatCurrency(managerDiscount,currencySymbol)}</strong></div><div class="kv total"><span>Total discount</span><strong>${formatCurrency(totalDiscount,currencySymbol)}</strong></div>${invoice.creditNoteAmount ? `<div class="kv"><span>Credit note</span><strong>${formatCurrency(Number(invoice.creditNoteAmount),currencySymbol)}</strong></div>` : ''}<div class="kv grand"><span>${paid ? 'Invoice Total' : 'Quotation Total'}</span><strong>${formatCurrency(Number(invoice.grandTotal || 0),currencySymbol)}</strong></div>`;
    popupWindow.document.write(`<!doctype html><html><head><title>${label} ${number}</title><meta charset="utf-8"><style>body{font-family:Arial,sans-serif;margin:0;padding:30px;color:#111827;background:#f8fafc}.page{max-width:1100px;margin:auto;background:white;padding:28px;box-shadow:0 2px 12px #cbd5e1}.top{display:flex;justify-content:space-between;border-bottom:2px solid #111827;padding-bottom:18px}.top h1{margin:0;font-size:26px}.top h2{margin:5px 0;font-size:18px}.muted{color:#64748b;font-size:12px}.badge{display:inline-block;padding:6px 10px;border-radius:999px;background:#ecfdf5;color:#047857;font-size:11px;font-weight:700;margin-top:8px}.section{border:1px solid #e2e8f0;border-radius:10px;padding:15px;margin-top:16px}.section h3{margin:0 0 12px;font-size:14px;text-transform:uppercase;letter-spacing:.04em}.grid2{display:grid;grid-template-columns:1fr 1fr;gap:14px}.kv{display:flex;justify-content:space-between;gap:15px;border-bottom:1px solid #f1f5f9;padding:7px 0;font-size:12px}.kv span{color:#64748b}.kv strong{text-align:right}.remarks{margin-top:10px;background:#f8fafc;border-radius:7px;padding:10px;font-size:12px}.remarks p{margin:5px 0;color:#475569}.workflow{display:grid;grid-template-columns:repeat(4,1fr);gap:8px}.step{border:1px solid #e2e8f0;border-radius:9px;padding:12px}.step b{display:block;font-size:11px}.step span{display:block;margin-top:5px;font-size:11px;color:#475569}.step.done{border-color:#86efac;background:#f0fdf4}.step.pending{border-color:#fcd34d;background:#fffbeb}.step.blocked{background:#f8fafc}.table{width:100%;border-collapse:collapse;font-size:12px}.table th,.table td{border-bottom:1px solid #e2e8f0;padding:9px;text-align:left}.table th{background:#f1f5f9}.right{text-align:right!important}.totals{margin-left:auto;max-width:420px}.total{font-weight:700;border-top:1px solid #cbd5e1;margin-top:5px}.grand{font-size:17px;font-weight:800;border-top:2px solid #111827;margin-top:6px;padding-top:10px}.print{margin-top:22px;padding:10px 16px;border:0;border-radius:7px;background:#111827;color:white;font-weight:700;cursor:pointer}@media(max-width:700px){.grid2,.workflow{grid-template-columns:1fr 1fr}}@media print{body{background:white;padding:0}.page{box-shadow:none;padding:0}.print{display:none}}</style></head><body><div class="page"><div class="top"><div><h1>AVASurface Billing</h1><h2>${label}</h2><div class="muted">${number} · ${formatDateTime(invoice.date)}</div></div><div><div class="muted">WORKFLOW STATUS</div><div class="badge">${invoice.workflowStatus || invoice.status}</div></div></div>${section('Customer & Salesperson',`<div class="grid2"><div><div class="muted">CUSTOMER</div><b>${invoice.customer?.name || '—'}</b><div>${invoice.customer?.phone || '—'}</div><div>${invoice.customer?.billingAddress || invoice.customer?.address || '—'}</div></div><div><div class="muted">SALESPERSON</div><b>${invoice.salespersonName || '—'}</b><div>${invoice.salespersonMobile || '—'}</div></div></div>`)}${section('Workflow',workflow)}${section('Manager Approval',managerBody)}${section('Promotions',promoBody)}${canSeeCreditNote ? section('Credit Note',creditBody) : ''}${section('Items',`<table class="table"><thead><tr><th>Item</th><th>Qty</th><th>Rate</th><th class="right">Total</th></tr></thead><tbody>${itemRows || '<tr><td colspan="4">No item details available.</td></tr>'}</tbody></table>`)}${section('Commercial Summary',`<div class="totals">${totalsBody}</div>`)}${paid ? section('Payment',paymentBody) : section('Payment',`<div class="kv"><span>Status</span><strong>AWAITING ACCOUNTS PAYMENT</strong></div><div class="kv"><span>Requested method</span><strong>${invoice.paymentMethodRequested || invoice.paymentMethod || '—'}</strong></div>`)}<button class="print" onclick="window.print()">Print ${label}</button></div></body></html>`);
    popupWindow.document.close(); popupWindow.focus();
  };

  const documentLabel = documentType === 'INVOICES' ? 'Invoice' : 'Quotation';
  return <div className="space-y-5">
    <div className="bg-slate-900 border border-slate-800 rounded-3xl p-5 flex flex-col lg:flex-row lg:items-center justify-between gap-4"><div><div className="flex items-center gap-2"><Receipt className="w-6 h-6 text-indigo-400" /><h2 className="text-xl font-black text-white">Quotations &amp; Invoices</h2></div><p className="text-xs text-slate-400 mt-1">Unconfirmed payments remain as Quotations. Once Accounts confirms payment, the document moves to Invoices.</p></div><button onClick={() => void refreshFromServer()} disabled={refreshing} className="px-3 py-2 rounded-xl bg-slate-800 text-slate-300 text-xs font-black flex items-center gap-2"><RefreshCw className={`w-3.5 h-3.5 ${refreshing ? 'animate-spin' : ''}`} />Refresh</button></div>
    <div className="grid grid-cols-1 md:grid-cols-2 gap-3"><button onClick={() => { setDocumentType('QUOTATIONS'); setStatus('ALL'); }} className={`p-4 rounded-2xl border text-left ${documentType === 'QUOTATIONS' ? 'bg-amber-950/30 border-amber-500/50' : 'bg-slate-900 border-slate-800'}`}><span className="text-xs font-black uppercase text-amber-300">Quotations</span><div className="text-2xl font-black text-white mt-1">{quotationCount}</div></button><button onClick={() => { setDocumentType('INVOICES'); setStatus('ALL'); }} className={`p-4 rounded-2xl border text-left ${documentType === 'INVOICES' ? 'bg-emerald-950/30 border-emerald-500/50' : 'bg-slate-900 border-slate-800'}`}><span className="text-xs font-black uppercase text-emerald-300">Invoices</span><div className="text-2xl font-black text-white mt-1">{invoiceCount}</div></button></div>
    <div className="flex flex-col md:flex-row gap-3 md:items-center md:justify-between"><div className="flex gap-2 bg-slate-900 p-1 rounded-xl border border-slate-800 w-fit"><button onClick={() => setMode('ALL')} className={`px-4 py-2 rounded-lg text-xs font-black ${mode === 'ALL' ? 'bg-indigo-600 text-white' : 'text-slate-400'}`}>All</button><button onClick={() => setMode('TODAY')} className={`px-4 py-2 rounded-lg text-xs font-black ${mode === 'TODAY' ? 'bg-emerald-600 text-white' : 'text-slate-400'}`}><Calendar className="inline w-3 h-3 mr-1" />Today</button></div><div className="flex gap-3 flex-1 md:justify-end"><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder={`Search ${documentLabel.toLowerCase()}, customer, salesperson, item...`} className="md:w-[420px] px-4 py-3 rounded-xl bg-slate-900 border border-slate-800 text-white text-xs" /><select value={status} onChange={(e) => setStatus(e.target.value as 'ALL' | InvoiceStatus)} className="md:w-[180px] px-3 py-3 rounded-xl bg-slate-900 border border-slate-800 text-white text-xs"><option value="ALL">All Status</option><option value="UNPAID">Unpaid</option><option value="PARTIAL">Partial</option><option value="PAID">Paid</option><option value="REFUNDED">Refunded</option></select></div></div>
    {visibleInvoices.length === 0 ? <div className="p-10 text-center bg-slate-900 border border-slate-800 rounded-3xl text-slate-500">No {documentLabel.toLowerCase()}s found.</div> : <div className="grid gap-4 lg:grid-cols-[420px_1fr]"><div className="bg-slate-900 border border-slate-800 rounded-2xl p-3 space-y-2 max-h-[700px] overflow-y-auto">{visibleInvoices.map((invoice) => <button key={invoice.id} onClick={() => setSelectedId(invoice.id)} className={`w-full text-left p-3 rounded-xl border ${selectedId === invoice.id ? 'border-indigo-500 bg-indigo-950/30' : 'border-slate-800'}`}><div className="flex items-center justify-between gap-3"><span className="font-mono font-black text-white text-sm">{invoice.invoiceNumber || invoice.quotationNumber}</span>{statusBadge(invoice.status)}</div><div className="mt-1 text-xs text-slate-300">{invoice.customer?.name || 'Customer'}</div><div className="mt-1 text-[10px] text-slate-500">{formatDateTime(invoice.date)} · {invoice.salespersonName || 'No salesperson'}</div><div className="mt-2 text-right text-sm font-black text-white">{formatCurrency(invoice.grandTotal, currencySymbol)}</div></button>)}</div><div className="bg-slate-900 border border-slate-800 rounded-2xl p-5">{!selected ? <div className="min-h-60 flex items-center justify-center text-slate-500">Select a {documentLabel.toLowerCase()} to view details.</div> : <div className="space-y-5"><div className="flex flex-col md:flex-row md:items-start justify-between gap-4"><div><div className="text-[10px] uppercase tracking-wider text-indigo-300 font-black">{documentLabel}</div><div className="text-xl font-black text-white">{selected.invoiceNumber || selected.quotationNumber}</div><div className="text-xs text-slate-400 mt-1">{selected.customer?.name || 'Customer'} · {selected.customer?.phone || 'No phone'}</div><div className="text-xs text-slate-500 mt-1">Salesperson: {selected.salespersonName || '—'}</div></div><div className="text-right"><div className="text-[10px] uppercase text-slate-500">Value</div><div className="text-2xl font-black text-white">{formatCurrency(selected.grandTotal, currencySymbol)}</div>{statusBadge(selected.status)}</div></div>{documentType === 'QUOTATIONS' && <div className="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-4 gap-3"><div className="p-4 rounded-2xl bg-violet-950/30 border border-violet-500/30"><div className="flex items-center gap-2 text-violet-200 font-black text-xs"><UserCheck className="w-4 h-4" />Manager Approval</div><div className="mt-2">{managerBadge(selected)}</div><div className="text-[10px] text-slate-400 mt-2">Discount: {Number(selected.branchManagerDiscountPercent || 0).toFixed(2)}% · {formatCurrency(Number(selected.branchManagerDiscountAmount || 0), currencySymbol)}</div></div><div className="p-4 rounded-2xl bg-fuchsia-950/30 border border-fuchsia-500/30"><div className="flex items-center gap-2 text-fuchsia-200 font-black text-xs"><Tag className="w-4 h-4" />Promotions</div><div className="mt-2 text-sm font-black text-white">{selected.promoCodeApplied || 'No promotion applied'}</div><div className="text-[10px] text-slate-400 mt-1">Discount: {formatCurrency(Number(selected.promoDiscountAmount || 0), currencySymbol)}</div></div><div className="p-4 rounded-2xl bg-amber-950/30 border border-amber-500/30"><div className="flex items-center gap-2 text-amber-200 font-black text-xs"><ShieldCheck className="w-4 h-4" />Credit Note</div><div className="mt-2 text-sm font-black text-white">{formatCurrency(Number(selected.creditNoteAmount || 0), currencySymbol)}</div><div className="mt-1">{creditBadge(creditNoteStatus(selected))}</div></div><div className="p-4 rounded-2xl bg-slate-950 border border-slate-800"><div className="flex items-center gap-2 text-slate-300 font-black text-xs"><Timer className="w-4 h-4" />Workflow</div><div className="mt-2 text-sm font-black text-white">{selected.workflowStatus || 'PAYMENT_PENDING'}</div></div></div>}{documentType === 'QUOTATIONS' && <div className="p-4 rounded-2xl bg-slate-950 border border-slate-800"><div className="text-xs font-black text-white mb-3">Quotation Workflow</div><div className="grid grid-cols-1 md:grid-cols-4 gap-2 text-[10px]"><div className="p-3 rounded-xl border border-slate-800"><div className="text-slate-500">1. POS Created</div><div className="text-white font-bold mt-1">Quotation {selected.quotationNumber || '—'}</div></div><div className="p-3 rounded-xl border border-emerald-500/40 bg-emerald-950/20"><div className="text-slate-500">2. Manager</div><div className="text-white font-bold mt-1">{managerStatus(selected)}</div></div><div className="p-3 rounded-xl border border-slate-800"><div className="text-slate-500">3. Accounts</div><div className="text-white font-bold mt-1">{selected.workflowStatus === 'PAYMENT_PENDING' ? 'READY FOR PAYMENT' : 'WAITING'}</div></div><div className="p-3 rounded-xl border border-slate-800"><div className="text-slate-500">4. Invoice</div><div className="text-white font-bold mt-1">{isFinalInvoice(selected) ? selected.invoiceNumber : 'Not generated yet'}</div></div></div></div>}<div className="overflow-x-auto rounded-xl border border-slate-800"><table className="w-full text-xs"><thead className="bg-slate-950 text-slate-400"><tr><th className="p-3 text-left">Item</th><th className="p-3 text-right">Qty</th><th className="p-3 text-right">Rate</th><th className="p-3 text-right">Total</th></tr></thead><tbody>{selected.items.map((item) => <tr key={`${selected.id}-${item.product.id}`} className="border-t border-slate-800"><td className="p-3 text-white">{item.product.name}</td><td className="p-3 text-right text-slate-300">{item.quantity} {item.selectedUnit || item.product.unit}</td><td className="p-3 text-right text-slate-300">{formatCurrency(item.finalUnitPrice, currencySymbol)}</td><td className="p-3 text-right text-white font-bold">{formatCurrency(item.totalPrice, currencySymbol)}</td></tr>)}</tbody></table></div><div className="grid grid-cols-1 md:grid-cols-3 gap-3"><div className="p-3 rounded-xl bg-slate-950 border border-slate-800"><div className="text-[10px] text-slate-500">Subtotal</div><div className="font-black text-white">{formatCurrency(selected.subtotal, currencySymbol)}</div></div><div className="p-3 rounded-xl bg-slate-950 border border-slate-800"><div className="text-[10px] text-slate-500">Total Discount</div><div className="font-black text-violet-300">{formatCurrency((selected.itemDiscountsTotal || 0) + (selected.promoDiscountAmount || 0) + (selected.branchManagerDiscountAmount || 0), currencySymbol)}</div></div><div className="p-3 rounded-xl bg-slate-950 border border-slate-800"><div className="text-[10px] text-slate-500">{documentType === 'INVOICES' ? 'Paid' : 'Payment Status'}</div><div className="font-black text-emerald-300">{documentType === 'INVOICES' ? formatCurrency(selected.amountPaid, currencySymbol) : selected.workflowStatus === 'PAYMENT_PENDING' ? 'READY FOR ACCOUNTS' : 'AWAITING MANAGER'}</div></div></div><div className="flex justify-end gap-2"><button onClick={() => openDocumentView(selected)} className="px-4 py-2 rounded-xl bg-indigo-600 hover:bg-indigo-500 text-white text-xs font-black flex items-center gap-2"><Eye className="w-4 h-4" /> View {documentLabel}</button>{documentType === 'INVOICES' && <button onClick={() => onSelectInvoiceToPrint(selected)} className="px-4 py-2 rounded-xl bg-slate-700 text-white text-xs font-black flex items-center gap-2"><Printer className="w-4 h-4" /> Print Invoice</button>}</div></div>}</div></div>}
  </div>;
};